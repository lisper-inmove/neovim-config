# NeovimConfig

1. pip install neovim "python-lsp-server[all]"
1. mkdir -p $HOME/.config && ln -s $HOME/neovim-config $HOME/.config/nvim
2. mkdir -p $HOME/.local/share/nvim/site/pack/packer/start/ && git clone git@github.com:wbthomason/packer.nvim.git $HOME/.local/share/nvim/site/pack/packer/start/packer.nvim
